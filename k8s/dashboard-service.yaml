apiVersion: v1
kind: Service
metadata:
  name: demand-dashboard-service
spec:
  selector:
    app: demand-dashboard
  ports:
    - protocol: TCP
      port: 8501
      targetPort: 8501
  type: ClusterIP
